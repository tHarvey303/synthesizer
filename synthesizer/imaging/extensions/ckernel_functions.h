/******************************************************************************
 * C function prototypes for quick kernel computations.
 * For now the usage of these is isolated to image creation.
 *****************************************************************************/

float uniform(float r);
float sph_anarchy(float r);
float gadget_2(float r);
float cubic(float r);
float quintic(float r);
